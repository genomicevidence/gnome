<h2>Resend unlock instructions</h2>

<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post, :class => "form-stacked actions" }) do |f| %>
  <%= devise_error_messages! %>

  <%= f.label :email %><br />
  <p><%= f.email_field :email %></p>

  <p><%= f.submit "Resend unlock instructions", :class => "btn primary" %></p>
  <%= render :partial => "devise/shared/links" %>
<% end %>

